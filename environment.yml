name: surface-app
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - playwright
      - google-api-python-client
      - google-auth
      - google-auth-httplib2
      - google-auth-oauthlib
